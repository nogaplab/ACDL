BasicRAG[@T]: {
    S: INSTRUCTIONS
    U: {
        name docs := k_relevant_docs(env.user_input[@T])
        ForEach(i: range(1, $docs.len)) {
          $docs[i].source
          $docs[i].content
        }
        ANSWER_QUESTION_BASED_ON_DOCS
        env.user_input[@T]
    }
}