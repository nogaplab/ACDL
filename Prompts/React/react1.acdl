React1[@t]: {
    S: TASK_DESC  // you are an assistant who's job is...
    S: AVAILABLE_TOOLS
    U: {env.user_question}
    ForEach(i: range(1,t-1)) {
        A: sys.tool_used[@i]
        T: sys.tool_used[@i].tool_response[@i]
    }
    S: INSTRUCTION   // choose an action, use ReACT
}